{
  "name": "student-onboarding-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Demo-ready onboarding hub for student programmers.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
